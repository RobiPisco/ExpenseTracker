# ExpenseTracker
App able to track income, expenses and check the total balance

Save data in local storage

Data color coded:
expenses in red
income in green

When expeses are written in the Amount field, has to be written with the minus (-) in front of the amount (e.g. -30)

Are used high order Array method .filter() .reduce() .map()
